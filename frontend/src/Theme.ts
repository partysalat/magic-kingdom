import { createTheme, ThemeOptions } from '@mui/material';

export const themeOptions: ThemeOptions = createTheme({
  palette: {
    primary: {
      main: '#3f51b5',
    },
    secondary: {
      main: '#f50057',
    },
  },
});
